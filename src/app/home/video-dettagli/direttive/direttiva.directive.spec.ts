import { DirettivaDirective } from './direttiva.directive';

describe('DirettivaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirettivaDirective();
    expect(directive).toBeTruthy();
  });
});
